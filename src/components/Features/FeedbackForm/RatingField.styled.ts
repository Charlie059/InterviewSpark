import styled from 'styled-components'
import { Grid, Typography } from '@mui/material'

export const RatingFieldContainer = styled(Grid)`
  margin-bottom: 20px;
`

export const ErrorText = styled(Typography)`
  color: red;
  font-size: 13px;
`
