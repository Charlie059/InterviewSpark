import { useState } from 'react'
import { Meta, ArgsTable, Canvas, Story } from '@storybook/addon-docs/blocks'
import CustomDialog from './CustomDialog'
import Button from '@mui/material/Button'
import Rating from '../../rating'
import Typography from '@mui/material/Typography'
import Chip from '@mui/material/Chip'

<Meta title='Components/CustomDialog' component={CustomDialog} />

# CustomDialog

The `CustomDialog` is a highly reusable component that displays a dialog with a title and message. It includes buttons for confirming or canceling the action and can be easily customized according to the needs of the interface.

## Props

Use this component wherever you need to get confirmation from the user or inform them about important information.

- `open`: Indicates whether the dialog is open or not.
- `onClose`: Callback function invoked when the dialog needs to be closed.
- `children`: The content to be displayed in the dialog.
- `actions`: The actions displayed at the bottom of the dialog.
- `sx`: The sx props for the dialog.

<ArgsTable of={CustomDialog} />

## Usage

Here is the default dialogue:

<Canvas>
  <Story name='PrimaryDialog' args={{ title: 'Title' }}>
    {args => {
      const [open, setOpen] = useState(false)
      const handleOpen = () => setOpen(true)
      const handleClose = () => setOpen(false)

      return (
        <>
          <Button onClick={handleOpen}>Show Default Dialogue</Button>
          <CustomDialog {...args} open={open} onClose={handleClose} />
        </>
      )
    }}

  </Story>
</Canvas>

In order to embed more components, we should modify the componentList.
For example, if a list of header is required to be displayed through the Custom Dialogue,
the prop `contentList` was modified as

you can try the example below:

<Canvas>
  <Story name='Example1' args={{ title: 'Title' }}>
    {args => {
      const [open, setOpen] = useState(false)
      const handleOpen = () => setOpen(true)
      const handleClose = () => setOpen(false)

      return (
        <>
          <Button onClick={handleOpen}>Show Custom Dialogue</Button>
          <CustomDialog {...args} open={open} onClose={handleClose}
          children={
            <>
              <Typography>Header 1</Typography>
              <Chip label="Chip Filled 1" />
              <Button>button3</Button>
            </>
            }/>
        </>
      )
    }}

  </Story>
</Canvas>

Here is another example about a rating dialogue:

<Canvas>
  <Story name='Example2' args={{ title: 'Title' }}>
    {args => {
      const [open, setOpen] = useState(false)
      const handleOpen = () => setOpen(true)
      const handleClose = () => setOpen(false)

      return (
        <>
          <Button onClick={handleOpen}>Show Rating Dialogue</Button>
          <CustomDialog {...args} open={open} onClose={handleClose}
                        children={
                          <>
                          <Rating/>
                          <Button sx={{
    display: 'block',
    margin: 'auto',
}} onClick={console}>Submit</Button>

</>
}/>
</>
)
}}

  </Story>
</Canvas>
